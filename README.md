Example application how to handle `PasswordBox` in application which uses [ReactiveUI](https://www.reactiveui.net/).

Class `PasswordHelper` source comes from https://www.wpftutorial.net/PasswordBox.html
